; Set awy to one
(define awy 1)

; Then see what awy is
awy

(cons 1 2)

(define marc (cons 3 4))

marc

(car marc)
(cdr marc)

(define finnegan #f)
finnegan

(define true #t)

(define bools (cons true finnegan))
(car bools)
(cdr bools)

(boolean? #t)
(boolean? #f)
(boolean? 1)
(boolean? marc)

(number? 1)
(number? awy)
(number? #t)
(number? #f)
; bug here  (number? #\s)
(number? #\space)

(pair? marc)
(pair? #t)
(pair? #f)
(pair? #\newline)
(pair? bools)
(pair? awy)
