; Set awy to one
(define awy 1)

; Then see what awy is
awy

(cons 1 2)

(define marc (cons 3 4))

marc

(car marc)
(cdr marc)

(define finnegan #f)
finnegan

(define true #t)

(define bools (cons true finnegan))
(car bools)
(cdr bools)

(number? 1)
(number? awy)
(number #t)
(number #f)
(number #\space)
